import { CodeTransPipe } from './code-trans.pipe';

describe('CodeTransPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeTransPipe();
    expect(pipe).toBeTruthy();
  });
});
